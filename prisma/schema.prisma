generator client {
  provider      = "prisma-client"
  output        = "../generated/client"
  binaryTargets = ["native", "darwin-arm64"]
}

datasource db {
  provider = "postgresql"
}

enum Role {
  USER
  MANAGER
  CLUB_OWNER
}

enum InviteStatus {
  PENDING
  ACCEPTED
  REJECTED
  EXPIRED
  REVOKED
  REQUESTED
}

enum ReviewType {
  USER
  CLUB
  EVENT
}

enum BadgeType {
  EVENT
  ACHIEVEMENT
}

enum BadgeTier {
  BRONZE
  SILVER
  GOLD
  PLATINUM
  DIAMOND
}
